version: "2"

services:
  web:
    build: web
    ports:
    - 80
  db:
    build: db
    environment:
      POSTGRES_PASSWORD: verysecretpassword
  words:
    build: words
    environment:
      PGPASSWORD: verysecretpassword

